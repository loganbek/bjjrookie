{
  "name": "bjjrookie",
  "module": "index.ts",
  "type": "module",
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "@types/jest": "^29.5.6",
    "appwrite": "^13.0.0",
    "jest": "^29.7.0"
  },
  "scripts": {
    "install": "bun i",
    "test": "bun test",
    "build": "bun build ./src/index.ts --format esm --output ./dist/index.js && mkdir -p ./dist",
    "coverage": "bun coverage"
  },
  "devDependencies": {
    "netlify": "^13.1.10",
    "netlify-cli": "^16.9.1",
    "react": "^18.2.0",
    "bun-types": "latest"
  }
}
